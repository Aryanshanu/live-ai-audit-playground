/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/live-ai-audit-playground',
  assetPrefix: '/live-ai-audit-playground/',
  images: { unoptimized: true },
};

export default nextConfig;
